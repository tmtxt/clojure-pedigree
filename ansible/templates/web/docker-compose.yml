# GENERATED BY ANSIBLE, DO NOT EDIT
# EDIT THE TEMPLATE IN ansible/templates/web/docker-compose.yml
app-webserver:
  build: ./images/webserver
  container_name: {{ name_prefix }}-web-server

app-postgres:
  image: postgres:9.4.5
  container_name: {{ name_prefix }}-web-postgres
  ports:
    - "{{ db_port }}:5432"
  environment:
    POSTGRES_PASSWORD: {{ db_pass }}
    POSTGRES_DB: {{ db_name }}
