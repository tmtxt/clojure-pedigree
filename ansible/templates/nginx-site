# -*- mode: conf -*-
server {
       server_name           {{ server_name }};
       listen                80;
       client_max_body_size  10M;

       location / {
                proxy_pass http://localhost:{{ web_ring_host_port }};
       }
}
